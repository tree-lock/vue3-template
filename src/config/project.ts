/**
 * 项目配置
 * 所有需要用到下面这些配置的地方，都直接引用`config.xxx`即可，并且只修改这里
 */
export default {
  baseUrl: "/api",
  baseUrlPrefix: "http://localhost:8080",
  baseLocalUrl: "http://localhost:3000",
  version: "1.0.0",
};
